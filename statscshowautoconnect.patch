# HG changeset patch
# Parent 82b88fffa3706d86e8f7b9df3c363cff585b98e2

Show auto connect setting in STATS connect.

diff -r 82b88fffa370 ircd/s_err.c
--- a/ircd/s_err.c
+++ b/ircd/s_err.c
@@ -458,7 +458,7 @@
 /* 212 */
   { RPL_STATSCOMMANDS, "%s %u %u", "212" },
 /* 213 */
-  { RPL_STATSCLINE, "C %s %s %d %s %d %s %s", "213" },
+  { RPL_STATSCLINE, "C %s %c %s %d %s %d %s %s", "213" },
 /* 214 */
   { 0 },
 /* 215 */
diff -r 82b88fffa370 ircd/s_stats.c
--- a/ircd/s_stats.c
+++ b/ircd/s_stats.c
@@ -128,6 +128,7 @@
 	send_reply(sptr, RPL_STATSULINE, host);
       else if (tmp->status & CONF_SERVER)
 	send_reply(sptr, RPL_STATSCLINE, name,
+	           (tmp->flags & CONF_AUTOCONNECT) ? '+' : '-',
 	           unmask ? host : "*", port,
 	           unmask ? pass : "*", maximum,
 	           hub_limit, get_conf_class(tmp));
